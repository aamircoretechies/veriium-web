/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // `appDir` is enabled by default in Next.js App Router — remove to avoid warnings
  // experimental: { appDir: true }
};

module.exports = nextConfig;
